# swift-ollama-photo-captioner
A simple MacOS app to quickly caption photos using locally running models on Ollama.

To run the model run the following commands:

1) `ollama serve`
2) `ollama run llava`
